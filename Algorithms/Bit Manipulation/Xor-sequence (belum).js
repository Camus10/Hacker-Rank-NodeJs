let arrayXor = [0, 1, 3, 0, 4, 1, 7, 0, 8, 1, 11, 0, 12, 1, 15, 0, 16, 1, 19, 0, 20, 1, 23, 0, 24, 1];

function getLAndR(l, r){

    /*
    let counter;
    let sumXor = 0;
    for(counter = l; counter <= r; counter++){
        sumXor = sumXor ^ arrayXor[counter];    
    }
    console.log(sumXor);
    */

    
}

getLAndR(2, 4);
getLAndR(2, 8);
getLAndR(5, 9);